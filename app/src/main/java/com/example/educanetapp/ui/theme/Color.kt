package com.example.educanetapp.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales - Azul vibrante y moderno
val Primary80 = Color(0xFF92C7FF)  // Azul claro suave
val Primary40 = Color(0xFF0D47A1)  // Azul profundo
val Primary = Color(0xFF1976D2)    // Azul principal

// Colores secundarios - Verde/Turquesa energético
val Secondary80 = Color(0xFF80DEEA) // Turquesa claro
val Secondary40 = Color(0xFF00838F) // Turquesa oscuro
val Secondary = Color(0xFF00ACC1)   // Turquesa medio

// Colores terciarios - Naranja/Coral para acentos
val Tertiary80 = Color(0xFFFFAB91) // Coral claro
val Tertiary40 = Color(0xFFE64A19) // Naranja oscuro
val Tertiary = Color(0xFFFF6F00)   // Naranja vibrante

// Colores de fondo y superficie
val Background = Color(0xFFFAFBFF)      // Blanco azulado muy suave
val Surface = Color(0xFFFFFFFF)         // Blanco puro
val SurfaceVariant = Color(0xFFF1F4F9)  // Gris azulado muy claro

// Colores de texto
val OnPrimary = Color(0xFFFFFFFF)
val OnSecondary = Color(0xFFFFFFFF)
val OnTertiary = Color(0xFFFFFFFF)
val OnBackground = Color(0xFF1A1C1E)
val OnSurface = Color(0xFF1A1C1E)
val OnSurfaceVariant = Color(0xFF43474E)

// Colores para modo oscuro
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val DarkSurfaceVariant = Color(0xFF2C2C2C)
val DarkOnBackground = Color(0xFFE6E1E5)
val DarkOnSurface = Color(0xFFE6E1E5)

// Colores de éxito, error y advertencia
val Success = Color(0xFF4CAF50)
val Error = Color(0xFFE53935)
val Warning = Color(0xFFFFA726)